# GoMicroServiceScoreBoard

Score Board API for game scores. Store and return scores for Labyrinth Tower Defense.

Built using using GO and Docker. Used GO library Mux Router to build a RESTful compatible API.
